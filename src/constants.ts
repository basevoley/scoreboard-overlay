// Shared display constants used across scoreboard components

/** Size of the team logo/shirt flipper widget in both scoreboard variants */
export const FLIPPER_SIZE = '2.3rem';

/** Maximum number of timeouts per team per set */
export const MAX_TIMEOUTS = 2;
